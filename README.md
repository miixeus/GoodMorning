
  # Follow up on prompt

  This is a code bundle for Follow up on prompt. The original project is available at https://www.figma.com/design/of58jKu3WAfSV8hrPDH6YZ/Follow-up-on-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  